const protocol = window.location.protocol;
const hostname = window.location.hostname;

export const API_SERVER = `${protocol}//${hostname}:443`;

export const MEDIA_SERVER = `${protocol}//${hostname}:8443`;
